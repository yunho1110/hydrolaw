# HydroLaw-AI (하이드로로-AI)

> RAG(검색 증강 생성) 기반의 물환경 법령·지자체 하수도 조례 안내 도구

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.10%2B-blue.svg)](https://www.python.org/)

> ⚠️ **면책 조항 (Disclaimer)**
> 본 프로젝트는 법률 자문을 대체하지 않습니다. 제공되는 결과는 참고용 정보이며, 법적 판단이 필요한 사안은 반드시 환경 관련 전문가(변호사, 환경기술사, 관할 지자체 담당 부서 등)의 확인을 거치시기 바랍니다. 개발자는 본 도구의 출력 결과로 인해 발생하는 어떠한 법적 책임도 지지 않습니다.

---

## 프로젝트 소개

HydroLaw-AI는 복잡한 물환경 법령 및 지자체별 하수도 조례 정보를 영세 사업자와 소상공인이 쉽게 이해하고 준수할 수 있도록 돕는 공개 도구입니다. 사용자가 자연어로 자신의 업종·사업장 상황을 입력하면, RAG 파이프라인이 관련 법령을 검색하여 근거 조항과 함께 이해하기 쉬운 가이드라인을 생성합니다.

## 만든 이유

매번 개정되는 「물환경보전법」의 복잡한 배출허용기준(BOD, COD, SS 등)을 수동으로 찾아보고 해석하는 과정은 영세 사업자들에게 상당한 시간 부담이자 위반 리스크로 이어집니다. 이 프로젝트는 사용자가 간단한 자연어 질의만으로 최신 법령에 기반한 규제 준수 가이드라인을 즉시 확인할 수 있도록 하는 것을 목표로 시작되었습니다.

## 주요 기능

- **자연어 질의 분석**: 사용자가 입력한 상황(업종, 위치, 폐수 배출 형태 등)을 물환경 법령 및 배출허용기준 구조에 맞춰 분석
- **법적 근거 매칭**: 관련 법 조항과 맞춤형 대응 지침을 문서 형태로 제공
- **웹 챗봇 UI**: 소상공인이 터미널 없이 쓸 수 있는 브라우저 챗 인터페이스(`python3 serve.py`). 업종 버튼·자주 묻는 질문 칩 제공, 외부 CDN 없이 오프라인 시연 가능
- **지자체 조례 반영(시범)**: 순천시 하수도 관련 조례를 RAG 지식 베이스에 반영 — 전국 지자체 확장 가능한 구조 실증
- **법령 개정 자동 감지**: 국가법령정보센터 Open API로 시행규칙 개정을 주기 감시(`tools/law_watch.py` + GitHub Actions 주간 크론). 감지 시 경고만 하며 수치 데이터는 사람 검증 후 갱신
- **예시 기반 학습**: 레포지토리에 포함된 테스트 질의 예시(`examples/example_queries.md`)로 질문 작성법과 챗봇 사용법을 빠르게 습득 가능
- **재사용 가능한 RAG 파이프라인**: 법령 문서를 매번 수동 검색해야 하는 반복 업무를 줄이는 기본 검색 흐름 제공

## 데이터 출처 및 최신성

- 기반 법령: 「물환경보전법」 및 관련 시행령·시행규칙 (국가법령정보센터 기준)
- 지식 베이스 갱신 시점: **2026-07-04** 기준 최신 개정본 반영 (배출허용기준은 시행규칙 별표 13, 최종
  개정 2021. 12. 10. / BOD·TOC·SS·총질소·총인 수치는 2020. 1. 1.부터 적용되는 현행 기준)
- 세부 출처·확인 방법·확인일자는 [`data/SOURCES.md`](data/SOURCES.md)에서 항목별로 확인할 수 있습니다.
- 지자체 조례: 순천시 하수도사업 설치 조례(전라남도순천시조례 제2054호)를 시범 반영. 전국 확대는 로드맵 참조

> 법령은 수시로 개정되므로, 결과 확인 시 상단에 표기된 데이터 기준일을 반드시 확인하시기 바랍니다.

## 아키텍처 개요

```
사용자 자연어 입력
      │
      ▼
[질의 파서/전처리]
      │
      ▼
[검색(Retrieval)] ── 법령·조례 벡터 인덱스
      │
      ▼
[생성(Generation)] ── LLM 기반 응답 생성
      │
      ▼
근거 조항 + 대응 지침 (문서 출력)
```

### 하이브리드 구조: 숫자는 JSON, 맥락은 RAG

이 프로젝트의 핵심 설계 원칙은 "정확해야 하는 숫자"와 "맥락 설명"을 서로 다른 저장소로 분리하는
것입니다.

- **정확 조회가 필요한 수치**(BOD·TOC·SS·총질소·총인의 지역별·규모별 배출허용기준)는 오직
  `data/emission_standards.json`에만 존재하며, 파이프라인은 이 파일을 코드로 직접 조회해 값을
  가져옵니다. LLM이 검색된 텍스트 조각에서 숫자를 "추측"하도록 두지 않기 위한 장치입니다.
- **맥락·개념 설명**(법 조항의 취지, 지역구분·규모구분의 정의, 업종 분류, 행정처분 절차 등)은
  `data/laws/*.md`를 벡터 인덱스로 만들어 RAG로 검색합니다. 이 마크다운 문서에는 의도적으로
  배출허용기준 수치 표를 넣지 않으며, 수치가 필요한 자리는 "구체 수치는 배출허용기준 데이터(별표
  13) 조회 결과를 따름"이라는 문구로 대체되어 있습니다.

이렇게 분리해 두면 LLM이 검색 청크에 섞여 있는 숫자를 잘못 인용해 환각(hallucination)을 일으키는
경로 자체를 차단할 수 있습니다. 임베딩 모델·벡터스토어·검색 파라미터 등 RAG 파이프라인의 구체
구성은 `config.yaml`을 참고하세요.

## 설치 방법

```bash
git clone https://github.com/yunho1110/hydrolaw.git
cd hydrolaw

# 최소 실행 (내장 키워드 검색 + 무LLM 안내 모드, API 키 불필요)
pip install pyyaml

# 전체 기능 (벡터 검색 + LLM 답변 생성)
pip install -r requirements.txt
```

## 사용 방법

### 웹 챗봇 (권장 — 소상공인·시연용)

```bash
python3 run.py --rebuild-index    # 최초 1회: 법령 문서 색인
python3 serve.py                  # 기동 후 안내되는 주소를 브라우저로 열기
```

업종 버튼(세차장/식품제조/도금/세탁/축산)이나 자주 묻는 질문을 클릭하면 바로 답변을 받을 수
있습니다. LLM API 키(`OPENAI_API_KEY` 또는 `ANTHROPIC_API_KEY`)가 없어도 검증된 수치 표와
법령 근거로 답변합니다.

### CLI

1. 레포지토리에 포함된 예시 파일(`examples/`) 및 `data/` 구조(`data/emission_standards.json`,
   `data/laws/*.md`)를 확인합니다.
2. 자신의 업종, 사업장 위치, 폐수 배출 상황 등 필요한 내용을 자연어로 입력합니다.

   ```bash
   python3 run.py --query "세차장을 운영 중인데 BOD 기준이 궁금합니다"
   ```
3. 실행 후 최신 물환경보전법 데이터베이스를 바탕으로 생성된 결과 및 법적 출처를 확인합니다.

### 법령 개정 감시

```bash
python3 tools/law_watch.py --oc <법제처 Open API 이메일ID>   # 개정 감지 시 exit 1
```

[open.law.go.kr](https://open.law.go.kr)에서 무료로 발급받은 OC 키를 GitHub 저장소 시크릿
`LAW_OC`로 등록하면 매주 월요일 자동 감시(GitHub Actions)가 활성화됩니다. 개정이 감지되면
워크플로가 실패로 표시되어 알림을 받게 되며, 데이터 재검증 후 `--update-state`로 기준선을
갱신합니다.

## 예시

| 입력 질의 | 출력 예시 |
|---|---|
| "세차장 운영 중, 폐수 방류 기준이 궁금해요" | 관련 배출허용기준 + 근거 조항 + 지자체 조례 안내 |

## 앞으로의 계획

- [x] **사용 예시 추가**: 업종별 예상 질의 24종 (`examples/example_queries.md`)
- [x] **자동화 기능 개선(1단계)**: 국가법령정보센터 API 연동 법령 개정 자동 감지 (`tools/law_watch.py`)
- [x] **오류 처리 개선**: 수치 조작 방지 Fence(생성 후 숫자 검열) + 무API fallback + 면책 조항 상시 출력
- [x] **지역별 템플릿(시범)**: 순천시 하수도 조례 반영
- [ ] **지역별 템플릿 확대**: 5대 강 유역 및 전국 지자체별 하수도 조례에 맞는 특화 데이터 템플릿 제공
- [ ] **자동화 기능 개선(2단계)**: 개정 감지 후 지식 베이스 반자동 갱신(사람 검증 워크플로 포함)
- [ ] **README 문서 보완**: 2026년 물환경 공모전 출전 및 연계 계획에 따른 아키텍처 상세 내용 보완

## 기여 방법

이슈 등록, PR, 지자체 조례 데이터 제보 모두 환영합니다. 기여 가이드는 `CONTRIBUTING.md`를 참고해주세요.

## 라이선스

이 프로젝트는 [MIT License](LICENSE)를 따릅니다.

## 문의

프로젝트 관련 문의는 GitHub Issue 또는 yunho9735@gmail.com으로 연락 부탁드립니다.
